{
  "name": "host-stand",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A WebMCP-enabled restaurant host stand simulation for the OpenAI WebMCP Challenge.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/data.js && node --check src/engine.js && node --check src/webmcp.js && node --check src/app.js",
    "verify:browser": "python3 output/playwright/verify_browser_randomized.py",
    "verify:webmcp": "python3 output/playwright/verify_webmcp_client.py"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
